<Application x:Class="CalculadoraInteligente.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="UI/MainWindow.xaml">
    <Application.Resources>

        <!-- ===== XP CLASSIC PALETTE ===== -->
        <SolidColorBrush x:Key="AppAccent"       Color="#0054E3"/>
        <SolidColorBrush x:Key="AppTextMain"     Color="#000000"/>
        <SolidColorBrush x:Key="AppTextMuted"    Color="#404040"/>
        <SolidColorBrush x:Key="AppBorderSoft"   Color="#808080"/>
        <SolidColorBrush x:Key="AppBg"           Color="#D4D0C8"/>
        <SolidColorBrush x:Key="AppPanelBg"      Color="#C8C4BC"/>

        <!-- ===== INPUT FIELD (sunken inset) ===== -->
        <Style x:Key="ToolInputStyle" TargetType="TextBox">
            <Setter Property="Background"              Value="#FFFFFF"/>
            <Setter Property="Foreground"              Value="#000000"/>
            <Setter Property="CaretBrush"              Value="#000000"/>
            <Setter Property="BorderBrush"             Value="#808080"/>
            <Setter Property="BorderThickness"         Value="2,2,1,1"/>
            <Setter Property="Padding"                 Value="4,3"/>
            <Setter Property="FontSize"                Value="14"/>
            <Setter Property="FontFamily"              Value="Tahoma"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- ===== LABEL ===== -->
        <Style x:Key="ToolLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="#000000"/>
            <Setter Property="FontSize"    Value="12"/>
            <Setter Property="FontFamily"  Value="Tahoma"/>
            <Setter Property="Margin"      Value="0,5,0,2"/>
        </Style>

        <Style x:Key="ToolResultLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="#404040"/>
            <Setter Property="FontSize"    Value="12"/>
            <Setter Property="FontFamily"  Value="Tahoma"/>
        </Style>

        <Style x:Key="ToolResultValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground"  Value="#000080"/>
            <Setter Property="FontSize"    Value="15"/>
            <Setter Property="FontFamily"  Value="Courier New"/>
            <Setter Property="FontWeight"  Value="Bold"/>
        </Style>

        <!-- ===== XP 3-D RAISED BUTTON ===== -->
        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Background"          Value="#D4D0C8"/>
            <Setter Property="Foreground"          Value="#000000"/>
            <Setter Property="Padding"             Value="16,6"/>
            <Setter Property="FontSize"            Value="13"/>
            <Setter Property="FontFamily"          Value="Tahoma"/>
            <Setter Property="FontWeight"          Value="Bold"/>
            <Setter Property="Cursor"              Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Outer" BorderThickness="1" BorderBrush="#404040" SnapsToDevicePixels="True">
                            <Border x:Name="Hi" BorderThickness="2,2,1,1" BorderBrush="#FFFFFF">
                                <Border x:Name="Sh" BorderThickness="1,1,2,2" BorderBrush="#808080"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Sh" Property="Background" Value="#E8E4DC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Hi" Property="BorderBrush" Value="#808080"/>
                                <Setter TargetName="Sh" Property="BorderBrush" Value="#FFFFFF"/>
                                <Setter TargetName="Sh" Property="Background"  Value="#C0BDB5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== RADIO BUTTON ===== -->
        <Style x:Key="ToolRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground"  Value="#000000"/>
            <Setter Property="FontSize"    Value="12"/>
            <Setter Property="FontFamily"  Value="Tahoma"/>
            <Setter Property="Margin"      Value="0,0,16,0"/>
            <Setter Property="Cursor"      Value="Hand"/>
        </Style>

    </Application.Resources>
</Application>
